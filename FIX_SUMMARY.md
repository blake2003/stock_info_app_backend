# 證券代號篩選問題修正說明

## 問題診斷

### 問題現象
執行證券代號篩選時，無法篩選出符合條件的資料，篩選結果為空。

### 根本原因
經過測試分析，發現問題的根本原因是：

1. **CSV 標頭欄位名稱不一致**
   - TWSE API 回傳的 CSV 標頭使用的是**中文欄位名稱**（例如：「證券代號」）
   - 但篩選函數 `filter_stocks_by_codes` 固定使用**英文欄位名稱**（'Code'）
   - 當資料字典的 key 是「證券代號」時，使用 `stock.get('Code', '')` 會返回空字串
   - 空字串無法匹配任何證券代號，導致篩選結果為空

2. **程式碼邏輯分析**
   - 第79行：標頭標準化時只去除引號和空白，**保留中文欄位名稱**
   - 第96行：使用 `dict(zip(header, row))` 建立字典，key 為中文欄位名稱
   - 第137行：篩選函數使用 `stock.get('Code', '')`，假設欄位名稱是英文

## 解決方案

### 方案 1：統一欄位名稱（已實作）

在 `fetch_twse_market_day_all_csv` 函數中：
- 建立中文到英文的欄位名稱對照表
- 解析標頭時自動將中文欄位名稱轉換為英文
- 確保後續的篩選函數可以使用一致的欄位名稱

```python
field_name_mapping = {
    '日期': 'Date',
    '證券代號': 'Code',
    '代號': 'Code',
    '證券名稱': 'Name',
    # ... 其他欄位
}
header = [field_name_mapping.get(h, h) for h in header]
```

### 方案 2：改進篩選函數（已實作）

在 `filter_stocks_by_codes` 函數中：
- 自動偵測證券代號欄位名稱
- 支援多種可能的欄位名稱（Code、證券代號、代號等）
- 增加錯誤處理和日誌記錄

這樣即使欄位名稱沒有轉換，篩選函數也能自動適應。

## 修正內容

### 1. CSV 解析時的欄位名稱轉換

**位置**：`fetch_twse_market_day_all_csv` 函數，約第81-100行

**修改**：
- 新增欄位名稱對照表 `field_name_mapping`
- 在標準化標頭後，將中文欄位名稱轉換為英文
- 增加除錯日誌以追蹤欄位名稱轉換過程

### 2. 篩選函數的自動偵測

**位置**：`filter_stocks_by_codes` 函數，約第162-184行

**修改**：
- 自動偵測證券代號欄位名稱（優先順序：Code > 證券代號 > 代號）
- 支援模糊匹配（包含「代號」或「code」的欄位名稱）
- 增加錯誤處理和詳細日誌
- 如果找不到證券代號欄位，記錄錯誤並返回空列表

## 測試驗證

已建立測試腳本 `test_filter_analysis.py`，驗證了：

1. **測試案例 1**：英文欄位名稱 'Code' ✓ 成功
2. **測試案例 2**：中文欄位名稱 '證券代號' 
   - 修正前：✗ 失敗（0 筆）
   - 修正後：✓ 成功（3 筆）
3. **測試案例 3**：欄位名稱格式問題 ✓ 成功

## 使用方式

修正後的程式碼支援兩種方式：

### 方式一：使用統一後的英文欄位名稱（推薦）
由於 CSV 解析時已經將欄位名稱轉換為英文，篩選函數可以正常使用：

```python
STOCK_CODES_TO_FILTER = ['0050', '2330', '2317']
market_data_list = filter_stocks_by_codes(market_data_list, STOCK_CODES_TO_FILTER)
```

### 方式二：自動偵測欄位名稱（備援）
即使欄位名稱沒有轉換，篩選函數也能自動偵測並使用正確的欄位名稱。

## 注意事項

1. **欄位名稱對照表**：目前對照表涵蓋常見欄位，如果 API 新增欄位，可能需要更新對照表
2. **日誌記錄**：修正後會輸出更詳細的日誌，可用於追蹤問題
3. **向後相容**：修正後的程式碼向後相容，不影響現有功能

## 相關檔案

- `main.py` - 主要程式碼（已修正）
- `test_filter_analysis.py` - 問題分析測試腳本
- `test_filter_debug.py` - 詳細診斷測試腳本（需要實際 API 呼叫）

